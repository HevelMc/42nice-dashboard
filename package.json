{
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "prettier": "^3.2.5",
    "prettier-plugin-svelte": "^3.2.2",
    "tslib": "^2.7.0",
    "turbo": "^2.0.14"
  },
  "packageManager": "pnpm@8.15.6",
  "engines": {
    "node": ">=18"
  },
  "name": "42nice-dashboard",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "dependencies": {
    "@capacitor/android": "^6.2.0"
  }
}
